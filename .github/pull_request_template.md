## Summary

- What changed:
- Why it matters:

## Verification

Run the relevant checks and paste the result:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\preflight-github-launch.ps1 -SkipLicenseCheck
```

For UI changes, include rendered evidence or screenshot paths.

## Public Boundary

Confirm:

- [ ] I did not add `public-data/`, `output/`, local databases, media, raw captions, logs, cookies, tokens, or secrets.
- [ ] I checked `docs/PUBLICATION_STAGING_PLAN.md`.
- [ ] I linked original creator/source attribution where relevant.
- [ ] Full third-party transcripts remain private/local by default.

## Remaining Risk

- Untested areas:
- Follow-up needed:
