# Night Run 2026-06-08

## Objective

Move Base2026 toward a usable public product:

- match the visual system of the main WordPress site at `https://aggressorbulkit.online/`;
- keep the search UI functional and readable;
- prepare intake for new TikTok creators:
  - `https://www.tiktok.com/@joshuamaraney`
  - `https://www.tiktok.com/@webhivedigital`
- preserve the public/private boundary and GPT review plan.

## Main Site Style Reference

Observed production style:

- font: `Source Sans 3`;
- background: warm off-white `#f7f4ee`;
- text: dark charcoal `#111820` / `#1f2933`;
- primary CTA orange: `#c84f07`;
- primary dark button: `#10231f`;
- button/input radius: `8px`;
- navigation: white sticky header, compact pill hover states, restrained borders;
- layout: wide centered container, calm spacing, no dark AI-app shell.

## Work Completed Locally

- replaced the dark knowledge UI shell with a light Alex Yarosh-compatible shell;
- changed public UI font to `Source Sans 3`;
- rebuilt header navigation to mirror the main site structure;
- changed CTA language to `Check My AI Visibility`;
- changed results heading to `Matched source passages`;
- restyled controls, chips, filters, result cards, pagination, dialog, and service pages;
- kept the required InstantSearch mount points:
  - `searchbox`
  - `selected-terms`
  - `current-refinements`
  - `author-refinement`
  - `source-refinement`
  - `year-refinement`
  - `stats`
  - `hits`
  - `pagination`
  - `transcript-dialog`

## Browser Preview Evidence

Because shell/deploy was blocked, the light style was temporarily injected into the current live `/knowledge/` page only for visual QA.

- preview screenshot: `output/evidence/knowledge-ay-light-preview.png`
- result: the interface visually aligns much more closely with the main site direction: warm light background, `Alex Yarosh` header, orange CTA, dark text, restrained cards, and readable source cards.

This screenshot does not prove the VPS has been deployed. It only validates the visual direction.

## Current Blockers

The local shell runner currently fails before command execution with:

`windows sandbox: spawn setup refresh`

Because of that, this session could not safely run:

- `git status`;
- local static server;
- package script;
- SSH deploy;
- TikTok intake commands;
- Python validation.

Do not treat the deployed VPS as updated until the release package is built and deployed in a shell-enabled run.

## Next Execution Path

When command execution is available:

1. Validate edited UI:
   - `node --check web/static/meili.js`
   - run a local static server for `web/static`;
   - screenshot desktop and mobile;
   - verify search results render with `AI Overviews`.
   - compare against `output/evidence/knowledge-ay-light-preview.png` and the main-site reference screenshot.
2. Build release:
   - run `scripts/package-public-release.ps1` with a new release name, for example `base2026-public-ay-style`.
3. Deploy release:
   - upload package to VPS;
   - switch `/var/www/base2026-knowledge/current`;
   - verify `/knowledge/` and `/knowledge-search/multi-search`.
4. New creator intake:
   - inspect existing TikTok source format;
   - add or refresh `@webhivedigital`;
   - add `@joshuamaraney`;
   - prefer platform captions first;
   - use local ASR fallback only when captions are missing;
   - run deterministic cleanup first;
   - use local/GPT-5.4 semantic cleanup only when transcript quality is poor and drift checks pass.
5. Rebuild public JSONL and reindex Meilisearch.
6. Update project memory and publication audit.

## Added Execution Artifacts

- `config/tiktok-intake-queue.20260608.json` — machine-readable queue for the discovered creators and video URLs.
- `scripts/tiktok-caption-browser-extract.mjs` — caption-first browser navigation extractor for TikTok video pages.
- `docs/schemas/TIKTOK_INTAKE_RECORD_SCHEMA.md` — normalized intake record contract.
- `docs/project-memory/TIKTOK_INTAKE_EXECUTION_PLAN.md` — phased execution plan from caption extraction to public export.
- `config/release-target.20260608-ay-style.json` — release target manifest for the light Alex Yarosh UI.

## Public Boundary Reminder

Generated exports, raw captions, audio/video, release zips, cookies, local DB files, and private research vaults are not GitHub-safe by default.
